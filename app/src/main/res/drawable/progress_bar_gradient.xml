<?xml version="1.0" encoding="utf-8"?>
<rotate xmlns:android="http://schemas.android.com/apk/res/android"
    android:duration="1000"
    android:fromDegrees="0"
    android:pivotX="50%"
    android:pivotY="50%"
    android:toDegrees="720">

    <shape
        android:innerRadius="14dp"
        android:shape="ring"
        android:thickness="5dp"
        android:useLevel="false">
        <size
            android:width="50dp"
            android:height="50dp" />

        <gradient
            android:centerColor="@color/loader_mid"
            android:centerY="0.5"
            android:endColor="@color/loader_end"
            android:startColor="@color/loader_start"
            android:type="sweep"
            android:useLevel="false" />
    </shape>

</rotate>